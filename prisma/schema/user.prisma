model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique 
  email     String   @unique 
  avatarUrl String?  // URL авы пользователя 
  password  String   

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt 
  reviews   Review[] 
}